interface Color {
  red: number;
  blue: number;
  green: number;
  yellow: number;
}
const color: Color = {
  red: 20,
  blue: 30,
  green: 40,
  yellow: 20
};

console.log(color);

export default color;
